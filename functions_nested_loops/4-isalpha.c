#include "main.h"
/**
 * _islpha - exercise
 *
 * @c: The character to be checked.
 *
 * Return: 1 if character is letter, 0 otherwise
 */

int _isalpha(int c)
{
	if ((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'))
		return (0);
	else
		return (0);
}
